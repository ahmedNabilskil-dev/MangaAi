// src/config/constants.ts

/**
 * The default project ID used for the in-memory data store.
 * This is defined here to avoid exporting constants from 'use server' files.
 */
export const DEFAULT_PROJECT_ID = 'proj-1';
